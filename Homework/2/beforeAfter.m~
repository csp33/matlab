function r = beforeAfter(day1,month1,day2,month2)
if month1 > month2
    before = "second";
elseif month1 < month2
    before = "first";
else
    if day1 > day2
        before = "second";
    elseif day2 < day1
        before = "first";
    else
        before = "";
    end
end
if before == ""
    fprintf("You entered the same dates!\n")
else
    fprintf("The date that happened first is the " + before + ".\n")
end